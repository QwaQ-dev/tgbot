services:
  test-bot:
    build: .
    container_name: test-bot
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      - postgres
    networks:
      - bot-network

volumes:
  postgres_data:

networks:
  bot-network:
    driver: bridge